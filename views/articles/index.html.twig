{% extends 'layouts/app.html.twig' %}

{% block content %}
    <h1>Articles</h1>
    <ul>
        {% for article in articles %}
            <li> <a href="/articles/{{ article.id }}">{{ article.title }}</a> by {{ article.author }}</li>
        {% endfor %}
    </ul>
{% endblock %}